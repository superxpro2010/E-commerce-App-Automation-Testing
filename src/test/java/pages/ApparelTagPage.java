package pages;

import org.openqa.selenium.WebDriver;

public class ApparelTagPage {
    private WebDriver driver;

    public ApparelTagPage(WebDriver driver) {
        this.driver = driver;
    }
}
